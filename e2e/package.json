{
  "name": "quadzero-scout-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Browser-based E2E smoke tests run against dev.scout.quadzero.com by the tester agent post-deploy.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install:browser": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.10.1"
  }
}
