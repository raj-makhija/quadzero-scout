# Pipeline Manager — drives one or more ticket transitions per Actions run.
#
# Triggers:
#   - cron every 5 min (safety net for missed events)
#   - issues.labeled                    (kicks pipeline when auto-pipeline label added)
#   - repository_dispatch [pipeline-tick] (fired by human-driven scripts)
#   - workflow_dispatch                 (manual trigger, optional ticket override)
#
# Note: issues.opened is intentionally NOT a trigger. It used to be, but every
# new ticket fired a doomed 1s run before the project-add and label
# automation completed. issues.labeled fires reliably the moment the
# auto-pipeline label is added (which happens immediately after open),
# so coverage is preserved without the noisy phantom runs.
#
# Each run drains the queue: it loops calling manager.sh while next-ticket.sh
# returns something. One Actions run can take a fresh ticket from new all the
# way to merged-to-develop. Loop bounded by iteration count + timeout-minutes.
#
# Auth:
#   - PIPELINE_TOKEN: classic PAT (repo + workflow + project) for gh ops
#   - CLAUDE_CODE_OAUTH_TOKEN: from `claude setup-token` on the user's laptop;
#     ties Claude usage to the Claude.ai Pro/Max subscription. If you'd
#     rather use the API, swap for ANTHROPIC_API_KEY.

name: Pipeline Manager

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:
    inputs:
      ticket:
        description: 'Optional issue number; if set, advance only that ticket by ONE state'
        required: false
        type: string
  issues:
    types: [labeled]
  repository_dispatch:
    types: [pipeline-tick]

concurrency:
  group: pipeline-manager
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  drain:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout develop (with PAT so later pushes work)
        uses: actions/checkout@v4
        with:
          ref: develop
          fetch-depth: 0
          token: ${{ secrets.PIPELINE_TOKEN }}

      - name: Configure git identity for any pipeline commits
        run: |
          git config user.name  "quadzero-pipeline[bot]"
          git config user.email "pipeline-bot@users.noreply.github.com"

      - name: Disable husky hooks (lint/commitlint) for unattended runs
        run: git config core.hooksPath /dev/null

      - name: Verify gh + jq
        env:
          GH_TOKEN: ${{ secrets.PIPELINE_TOKEN }}
        run: |
          gh --version
          jq --version
          gh auth status

      - name: Install Claude Code CLI
        run: |
          npm install -g @anthropic-ai/claude-code
          claude --version

      - name: Drain the queue
        env:
          GH_TOKEN: ${{ secrets.PIPELINE_TOKEN }}
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          PIPELINE_DEVELOPER_AGENT: claude
          PIPELINE_PR_REVIEWER_AGENT: claude
          PIPELINE_TESTER_AGENT: claude
          PIPELINE_MAX_ATTEMPTS: '3'
        run: |
          set -euo pipefail

          if [[ -n "${{ inputs.ticket }}" ]]; then
            echo "manager: single-step on requested ticket #${{ inputs.ticket }}"
            scripts/manager.sh "${{ inputs.ticket }}"
            exit 0
          fi

          for i in $(seq 1 25); do
            NEXT="$(scripts/next-ticket.sh | head -1 | awk '{print $1}' || true)"
            if [[ -z "$NEXT" ]]; then
              echo "manager: queue drained after $((i-1)) iterations"
              break
            fi
            echo "manager: iter $i, advancing #$NEXT"
            scripts/manager.sh
            sleep 3
          done
